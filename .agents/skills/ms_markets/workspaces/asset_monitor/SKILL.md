---
name: mainsequence-markets-asset-monitor-workspace
description: Legacy redirect. Use .agents/skills/ms_markets/command_center/asset_monitor/SKILL.md for ms-markets Command Center Asset Monitor helpers, provider API contracts, and workspace bindings.
---

# Main Sequence Markets Asset Monitor Workspace

This skill path is retained only as a redirect for older references.

Use the current skill:

```text
.agents/skills/ms_markets/command_center/asset_monitor/SKILL.md
```

The current flow uses `getAssetMonitorFrame` and direct
`core.tabular_frame@v1` output instead of `listAssets` plus a provider-native
response mapping.
